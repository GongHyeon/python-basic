 part.3 자료형(집합 자료형)
 
 1. 자료형(집합 자료형)
   (1) 집합은 다음과 같은 특징이있다.
       - 중복을 허용하지 않는다.
       - 순서가 없다.
 
   (2) 집합은 리스트 혹은 문자열을 이용해 초기화할 수 있다.
       - 이때 set() 함수를 이용한다.
 
   (3) 혹은 중괄호({})안에 각 원소를 콤마(,)를 기준으로 삽입함으로써 초기화 할 수 있다.
   (4) 데이터의 조회 및 수정에 있어서 O(1)의 시간에 처리할 수 있다.
 
 정리: list와 tuple => 순서가 있고, 인덱싱 개념이 존재한다.(값을 얻을 수 있다.)
      사전과 집합자료형 => 순서가 없고, 인덱싱으로 값을 얻을 수 없다.
      
      
 
 2. 집합 자료형의 연산 
 
   | : 합집합(전체)
   & : 교집합(공통)
   - : 차집합(빼기)
 
   ex) a=set([1,2,3,4]) , b=set([4,5,6])
   print(a|b) 
 
   => 출력 : {1,2,3,4,5,6}
   
   
   3. 집합 자료형 관련 함수
   
     data = set([1,2,3,4])
     data.add(5) => {1,2,3,4,5} 로 출력
   
     data.update(6,7) => {1,2,3,4,5,6,7} //새로운 원소를 여러개 추가하고자 할 때.
     data.remove(3) => {1,2,4,5,6,7} //특정한 값 삭제
